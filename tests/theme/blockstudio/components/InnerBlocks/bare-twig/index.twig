<InnerBlocks 
  style="padding: 1rem; background: #f1f1f1;"
  class="blockstudio-test__block test test2 test3"
  id="{{ b.name|replace({'/': '-'}) }}"
  tag="section"
  data-attr="console.log('click')"
  template="{{ [
    ['core/heading', { 'placeholder' : 'Book = Title' }],
    ['core/paragraph', { 'placeholder' : 'Summary' }]
  ]|json_encode|escape('html_attr') }}"
  allowedBlocks="{{ ['core/heading', 'core/paragraph']|json_encode|escape('html_attr') }}"
/>