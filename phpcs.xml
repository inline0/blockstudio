<?xml version="1.0"?>
<ruleset name="Blockstudio">
	<description>WordPress Coding Standards for Blockstudio v7</description>

	<!-- Files and directories to check -->
	<file>./blockstudio.php</file>
	<file>./includes/class-plugin.php</file>
	<file>./includes/classes/</file>
	<file>./includes/functions/</file>
	<file>./includes/interfaces/</file>
	<file>./tests/theme/</file>

	<!-- Exclude vendor, node_modules, and reference v6 code -->
	<exclude-pattern>*/vendor/*</exclude-pattern>
	<exclude-pattern>*/node_modules/*</exclude-pattern>
	<exclude-pattern>*/_reference/*</exclude-pattern>

	<!-- Use WordPress coding standards -->
	<rule ref="WordPress"/>

	<!-- Exclude class file naming - using PSR-4 style naming -->
	<rule ref="WordPress.Files.FileName.InvalidClassFileName">
		<exclude-pattern>*</exclude-pattern>
	</rule>
	<rule ref="WordPress.Files.FileName.NotHyphenatedLowercase">
		<exclude-pattern>*</exclude-pattern>
	</rule>

	<!-- Allow hook names with hyphens (public API, can't change) -->
	<rule ref="WordPress.NamingConventions.ValidHookName.UseUnderscores">
		<severity>0</severity>
	</rule>

	<!-- PHP 8.2+ -->
	<config name="testVersion" value="8.2-"/>

	<!-- Text domain -->
	<rule ref="WordPress.WP.I18n">
		<properties>
			<property name="text_domain" type="array">
				<element value="blockstudio"/>
			</property>
		</properties>
	</rule>

	<!-- Prefix for hooks, functions, classes -->
	<rule ref="WordPress.NamingConventions.PrefixAllGlobals">
		<properties>
			<property name="prefixes" type="array">
				<element value="blockstudio"/>
				<element value="Blockstudio"/>
			</property>
		</properties>
	</rule>
</ruleset>
