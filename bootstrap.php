<?php
/**
 * Composer autoload bootstrap.
 *
 * Loaded automatically via Composer's autoload.files when Blockstudio
 * is installed as a dependency (e.g. in a theme's vendor/ directory).
 *
 * @package Blockstudio
 */

if ( ! defined( 'ABSPATH' ) || defined( 'BLOCKSTUDIO_VERSION' ) ) {
	return;
}

require_once __DIR__ . '/blockstudio.php';
